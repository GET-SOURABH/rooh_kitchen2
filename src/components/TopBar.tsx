{/*  import React, { useState } from 'react';
import { XIcon } from './icons/Icons';

const TopBar = () => {
  const [isVisible, setIsVisible] = useState(true);

  if (!isVisible) return null;

  return (
    <div className="bg-foreground text-background py-2.5 px-4 text-center text-sm relative">
      <div className="container-main flex items-center justify-center gap-2">
        <span className="font-medium">
          ✨ Today's Special: <span className="text-primary font-semibold">Paneer Tikka Wrap</span> • Free eco-cutlery • Call to pre-order
        </span>
      </div>
      <button
        onClick={() => setIsVisible(false)}
        className="absolute right-4 top-1/2 -translate-y-1/2 p-1.5 hover:bg-background/10 rounded-full transition-colors"
        aria-label="Dismiss announcement"
      >
        <XIcon className="w-4 h-4" />
      </button>
    </div>
  );
};

export default TopBar;''' 
*/}
